#define _CRT_SECURE_NO_WARNIGS
#include <stdio.h>
void main()
{
	int a, b;

	for (; ;)
	{
		printf("더할 두 수 입력(멈추려면 Ctrl_C) : ");
		scanf_s("%d &d", &a, &b);

		printf("%d + %d = %d\n", a, b, a + b);
	}
}
