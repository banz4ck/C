#define _CRT_NO_WARNINGS
#include <stdio.h>
#include <string.h>

int main() {

    char str[100];
    int idx;

    printf("Enter a word: ");
    scanf_s("%s", str, sizeof(str));

    idx = strlen(str) / 2;
    printf("Result: %c", str[idx]);  
}
