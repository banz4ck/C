#define _CRT_NO_WARNINGS
#include <stdio.h>
#include <math.h>

void main()
{
	int x;

	printf("정수를 입력하세요.: ");
	scanf_s("%d", &x);

	printf("약수: ");
	for (int i = 1; i <= x; ++i)
	{
		if (x % i == 0) {
			printf("%d ", i); }
	}
		printf("\n");
}
