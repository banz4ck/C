#include <stdio.h>

void main()
{
    int i, k;
    
    for(i=1; i<=9; i++)
    {
        for(k=2; k<=9; k++)
        {
            printf("%2dX%2d=%2d",k, i, k*i); //정수를 두저리수로 출력. 두자리로 맞춰서 출력 (한자리수일시 공백 추가)
        }
        printf("\n");
    }
}
